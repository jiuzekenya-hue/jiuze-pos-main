import { useEffect, useState } from 'react'
import { Link } from 'react-router-dom'
import { useAuth } from '../contexts/auth-context'
import { can } from '../lib/permissions'
import { createCashier, deleteCashier, listBusinessUsers, type CashierUser } from '../services/cashierService'

export default function Users() {
  const { role } = useAuth(); const [users, setUsers] = useState<CashierUser[]>([]); const [loading, setLoading] = useState(true); const [saving, setSaving] = useState(false); const [removingUserId, setRemovingUserId] = useState<string | null>(null); const [error, setError] = useState(''); const [form, setForm] = useState({ email: '', password: '', fullName: '', phone: '' })
  const load = async () => { setLoading(true); setError(''); try { setUsers(await listBusinessUsers()) } catch (err) { setError(err instanceof Error ? err.message : 'Unable to load users.') } finally { setLoading(false) } }
  useEffect(() => { if (can(role, 'userManagement')) void load(); else setLoading(false) }, [role])
  if (!can(role, 'userManagement')) return <main className="min-h-screen bg-paper px-6 py-8"><div className="max-w-5xl mx-auto"><header className="mb-8"><p className="text-xs font-medium uppercase tracking-[0.16em] text-market-600">Team</p><h1 className="font-display font-semibold text-3xl text-ink mt-1">Users</h1></header><div className="rounded-xl border border-line bg-paper-raised p-8 text-center"><h2 className="font-display font-semibold text-xl text-ink">Access restricted</h2><p className="text-sm text-ink-muted mt-2">User management is available to business owners only.</p></div></div></main>
  const submit = async (event: React.FormEvent) => { event.preventDefault(); setSaving(true); setError(''); try { const created = await createCashier(form); setUsers((current) => [...current, created]); setForm({ email: '', password: '', fullName: '', phone: '' }) } catch (err) { setError(err instanceof Error ? err.message : 'Unable to create cashier.') } finally { setSaving(false) } }
  const remove = async (user: CashierUser) => { if (user.role !== 'cashier') return; if (!window.confirm(`Remove ${user.fullName || user.email || 'this cashier'}? This cannot be undone.`)) return; setRemovingUserId(user.id); setError(''); try { await deleteCashier(user.id); setUsers((current) => current.filter((currentUser) => currentUser.id !== user.id)) } catch (err) { setError(err instanceof Error ? err.message : 'Unable to remove cashier.') } finally { setRemovingUserId(null) } }
  return <main className="min-h-screen bg-paper px-5 py-7 sm:px-6"><div className="max-w-6xl mx-auto"><header className="flex items-end justify-between gap-4 mb-8"><div><p className="text-xs font-medium uppercase tracking-[0.16em] text-market-600">Team</p><h1 className="font-display font-semibold text-3xl text-ink mt-1 tracking-tight">Users</h1><p className="text-sm text-ink-muted mt-2">Manage the people who operate your store.</p></div><Link to="/" className="rounded-lg border border-line bg-paper-raised px-4 py-2.5 text-sm font-medium text-ink">Dashboard</Link></header>{error && <div role="alert" className="mb-6 rounded-lg border border-brick-200 bg-brick-50 px-4 py-3 text-sm text-brick-700">{error}</div>}<div className="grid gap-5 lg:grid-cols-[380px_1fr]"><form onSubmit={submit} className="bg-paper-raised border border-line rounded-xl p-5 h-fit"><p className="text-xs font-medium uppercase tracking-wide text-market-600">Staff access</p><h2 className="font-display font-semibold text-xl text-ink mt-1">Add cashier</h2><p className="text-sm text-ink-muted mt-1 mb-5">Create a login for a staff member.</p><div className="space-y-3"><input required type="email" value={form.email} onChange={(e) => setForm({ ...form, email: e.target.value })} placeholder="Email" aria-label="Email" className="field" /><input required type="password" minLength={8} value={form.password} onChange={(e) => setForm({ ...form, password: e.target.value })} placeholder="Temporary password (8+ characters)" aria-label="Password" className="field" /><input value={form.fullName} onChange={(e) => setForm({ ...form, fullName: e.target.value })} maxLength={100} placeholder="Full name" aria-label="Full name" className="field" /><input value={form.phone} onChange={(e) => setForm({ ...form, phone: e.target.value })} maxLength={50} placeholder="Phone (optional)" aria-label="Phone" className="field" /></div><button type="submit" disabled={saving} className="mt-4 w-full rounded-lg bg-ink px-4 py-2.5 text-sm font-medium text-paper disabled:opacity-50">{saving ? 'Creating…' : 'Create cashier'}</button></form><section className="bg-paper-raised border border-line rounded-xl overflow-hidden"><div className="px-5 py-4 border-b border-line flex items-center justify-between"><div><h2 className="font-display font-semibold text-lg text-ink">Business users</h2><p className="text-xs text-ink-muted mt-1">Owners and cashiers</p></div><span className="text-xs font-mono text-ink-muted">{users.length}</span></div>{loading ? <div className="px-5 py-12 text-center text-sm text-ink-muted">Loading users…</div> : users.length === 0 ? <div className="px-5 py-12 text-center text-sm text-ink-muted">No users found.</div> : <div className="overflow-x-auto"><table className="w-full text-sm"><thead><tr className="border-b border-line text-left text-xs uppercase tracking-wide text-ink-muted"><th className="px-5 py-3">Name</th><th className="px-3 py-3">Email</th><th className="px-3 py-3">Role</th><th className="px-3 py-3">Created</th><th className="px-5 py-3 text-right">Action</th></tr></thead><tbody className="divide-y divide-line">{users.map((user) => <tr key={user.id} className="hover:bg-paper"><td className="px-5 py-4 font-medium text-ink">{user.fullName || '—'}</td><td className="px-3 py-4 text-ink-muted">{user.email || '—'}</td><td className="px-3 py-4"><span className="rounded-full bg-market-50 px-2.5 py-1 text-xs capitalize text-market-700">{user.role}</span></td><td className="px-3 py-4 text-ink-muted">{new Date(user.createdAt).toLocaleDateString()}</td><td className="px-5 py-4 text-right">{user.role === 'cashier' && <button type="button" onClick={() => void remove(user)} disabled={removingUserId === user.id} className="text-xs font-medium text-brick-600 hover:underline disabled:opacity-50">{removingUserId === user.id ? 'Removing…' : 'Remove'}</button>}</td></tr>)}</tbody></table></div>}</section></div></div></main>
}
