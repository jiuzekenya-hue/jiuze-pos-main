import { useCallback, useEffect, useState } from 'react'
import { Link } from 'react-router-dom'
import { useAuth } from '../contexts/auth-context'
import { getSale, listSales, type SaleDetail, type SaleSummary } from '../services/salesHistoryService'
const money = (value: number) => `KES ${value.toFixed(2)}`
export default function SalesHistory() {
  const { profile } = useAuth(); const [sales, setSales] = useState<SaleSummary[]>([]); const [search, setSearch] = useState(''); const [selected, setSelected] = useState<SaleDetail | null>(null); const [loading, setLoading] = useState(true); const [error, setError] = useState<string | null>(null)
  const load = useCallback(async () => { if (!profile?.businessId) return; setLoading(true); setError(null); try { setSales(await listSales(profile.businessId, search)) } catch (err) { setError(err instanceof Error ? err.message : 'Unable to load sales.') } finally { setLoading(false) } }, [profile?.businessId, search]); useEffect(() => { void load() }, [load]); const openSale = async (saleId: string) => { if (!profile?.businessId) return; setError(null); try { setSelected(await getSale(profile.businessId, saleId)) } catch (err) { setError(err instanceof Error ? err.message : 'Unable to load sale.') } }; const printSale = () => { if (!selected) return; const win = window.open('', '_blank', 'width=420,height=720'); if (!win) return; const items = selected.items.map((item) => `<div style="display:flex;justify-content:space-between;margin:10px 0"><span>${item.quantity} × ${item.productName}<br><small>${money(item.unitPrice)} each</small></span><strong>${money(item.subtotal)}</strong></div>`).join(''); win.document.write(`<!doctype html><html><head><title>${selected.receiptNumber}</title><style>body{font-family:Arial,sans-serif;width:72mm;margin:0 auto;padding:12px;font-size:12px;color:#111}h1{text-align:center;font-size:18px;margin:0 0 4px}p{text-align:center;margin:4px 0;color:#555}.line{border-top:1px dashed #999;margin:12px 0}.row{display:flex;justify-content:space-between;margin:6px 0}.total{font-size:15px;font-weight:bold}</style></head><body><h1>JIUZEPOS</h1><p>Sales receipt</p><p><strong>${selected.receiptNumber}</strong></p><div class="line"></div>${items}<div class="line"></div><div class="row"><span>Subtotal</span><span>${money(selected.subtotal)}</span></div><div class="row"><span>Discount</span><span>${money(selected.discount)}</span></div><div class="row total"><span>Total</span><span>${money(selected.total)}</span></div><div class="row"><span>Payment</span><span>${selected.payment?.method.toUpperCase() ?? '—'}</span></div><div class="row"><span>Paid</span><span>${money(selected.payment?.amount ?? 0)}</span></div><div class="row total"><span>Change</span><span>${money(Math.max(0, (selected.payment?.amount ?? 0) - selected.total))}</span></div><div class="line"></div><p>Thank you for your purchase.</p><script>window.onload=()=>window.print()</script></body></html>`); win.document.close() }
  return <div className="min-h-screen bg-paper px-4 py-6 sm:px-6"><div className="max-w-6xl mx-auto"><div className="flex items-start justify-between gap-4 mb-6"><div><p className="text-xs font-mono uppercase tracking-wide text-market-600">Phase 4</p><h1 className="font-display font-semibold text-3xl text-ink mt-1">Sales history</h1><p className="text-sm text-ink-muted mt-2">Review completed transactions and receipts.</p></div><div className="flex gap-3 text-sm"><Link to="/" className="text-market-700">Home</Link><Link to="/checkout" className="text-market-700">New sale</Link><Link to="/products" className="text-market-700">Products</Link></div></div>{error && <div role="alert" className="mb-5 rounded-md border border-brick-200 bg-brick-50 px-4 py-3 text-sm text-brick-700">{error}</div>}<div className="mb-5"><input value={search} onChange={(e) => setSearch(e.target.value)} placeholder="Search receipt number" aria-label="Search sales" className="field w-full max-w-md" /></div>{loading ? <div className="rounded-lg border border-line bg-paper-raised px-5 py-12 text-center text-sm text-ink-muted">Loading sales…</div> : sales.length === 0 ? <div className="rounded-lg border border-line bg-paper-raised px-5 py-12 text-center text-sm text-ink-muted">No sales found.</div> : <div className="overflow-x-auto rounded-lg border border-line bg-paper-raised"><table className="w-full text-sm"><thead className="border-b border-line text-left text-xs uppercase tracking-wide text-ink-muted"><tr><th className="px-4 py-3">Date</th><th className="px-4 py-3">Receipt</th><th className="px-4 py-3">Total</th><th className="px-4 py-3">Status</th><th className="px-4 py-3">Action</th></tr></thead><tbody className="divide-y divide-line">{sales.map((sale) => <tr key={sale.id}><td className="px-4 py-3 text-ink-muted">{new Date(sale.createdAt).toLocaleString()}</td><td className="px-4 py-3 font-mono font-medium text-ink">{sale.receiptNumber}</td><td className="px-4 py-3 font-medium text-ink">{money(sale.total)}</td><td className="px-4 py-3 capitalize text-ink-muted">{sale.status}</td><td className="px-4 py-3"><button type="button" onClick={() => void openSale(sale.id)} className="text-market-700 font-medium">View</button></td></tr>)}</tbody></table></div>}{selected && <div className="fixed inset-0 z-50 bg-black/40 p-4 flex items-center justify-center" role="dialog" aria-modal="true"><div className="w-full max-w-lg max-h-[90vh] overflow-y-auto rounded-lg border border-line bg-paper-raised p-6"><div className="flex justify-between gap-4"><div><p className="text-xs uppercase tracking-wide text-market-600">Sales receipt</p><h2 className="font-display text-2xl font-semibold text-ink mt-1">{selected.receiptNumber}</h2><p className="text-xs text-ink-muted mt-1">{new Date(selected.createdAt).toLocaleString()}</p></div><button type="button" onClick={() => setSelected(null)} className="text-sm text-ink-muted">Close</button></div><div className="mt-5 divide-y divide-line">{selected.items.map((item) => <div key={item.id} className="py-3 flex justify-between gap-4"><div><p className="font-medium text-ink">{item.productName}</p><p className="text-xs text-ink-muted">{item.quantity} × {money(item.unitPrice)}</p></div><strong className="text-ink">{money(item.subtotal)}</strong></div>)}</div><div className="border-t border-line mt-2 pt-4 space-y-2 text-sm"><div className="flex justify-between"><span className="text-ink-muted">Subtotal</span><span>{money(selected.subtotal)}</span></div><div className="flex justify-between"><span className="text-ink-muted">Discount</span><span>{money(selected.discount)}</span></div><div className="flex justify-between text-lg font-semibold"><span>Total</span><span>{money(selected.total)}</span></div><div className="flex justify-between"><span className="text-ink-muted">Payment</span><span>{selected.payment?.method.toUpperCase() ?? '—'}</span></div><div className="flex justify-between"><span className="text-ink-muted">Paid</span><span>{money(selected.payment?.amount ?? 0)}</span></div><div className="flex justify-between font-semibold"><span>Change</span><span>{money(Math.max(0, (selected.payment?.amount ?? 0) - selected.total))}</span></div></div><button type="button" onClick={printSale} className="w-full mt-6 rounded-md bg-ink px-4 py-3 text-sm font-medium text-paper">Print receipt</button></div></div>}</div></div>
}
